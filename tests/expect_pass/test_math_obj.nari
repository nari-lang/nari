print("Math Tests\n");
print("math.pow(5, 3) = " @ math.pow(5, 3));
print("math.square(7) = " @ math.square(7));
print("math.sqrt(4) = " @ math.sqrt(4));
print("math.abs(-5) = " @ math.abs(-5));

// Function expressions in variable assignments
let double = func(x) {
    return x * 2;
};

let greet = func(name) {
    return "Hello, " @ name @ "!";
};

print("Variable Assignment Tests\n");
print("double(9) = " @ double(9));
print(greet("World"));

// Higher-order functions - passing functions as parameters
func apply_twice(fn, value) {
    return fn(fn(value));
}

let increment = func(n) {
    return n + 1;
};

print("Higher-Order Function Tests\n");
print("apply_twice(increment, 5) = " @ apply_twice(increment, 5));
print("apply_twice(double, 3) = " @ apply_twice(double, 3));

// Functions returning functions
func make_adder(n) {
    return func(x) {
        return x + n;
    };
}

print("Function Factory Tests\n");
let add_10 = make_adder(10);
let add_5 = make_adder(5);
print("add_10(7) = " @ add_10(7));
print("add_5(3) = " @ add_5(3));

// Complex nested object with function expressions
let calculator = {
    operations: {
        basic: {
            add: func(a, b) { return a + b; },
            subtract: func(a, b) { return a - b; }
        },
        advanced: {
            power: func(base, exp) {
                if (exp == 0) {
                    return 1;
                }
                let result = 1;
                let i = 0;
                while (i < exp) {
                    result = result * base;
                    i = i + 1;
                }
                return result;
            }
        }
    },
    apply_op: func(op_fn, a, b) {
        return op_fn(a, b);
    }
};

print("Nested Object Method Tests\n");
print("calculator.operations.basic.add(8, 2) = " @ calculator.operations.basic.add(8, 2));
print("calculator.operations.basic.subtract(8, 2) = " @ calculator.operations.basic.subtract(8, 2));
print("calculator.operations.advanced.power(2, 3) = " @ calculator.operations.advanced.power(2, 3));
print("calculator.apply_op(calculator.operations.basic.add, 15, 25) = " @ calculator.apply_op(calculator.operations.basic.add, 15, 25));

// Array of functions
let operations = [
    func(x) { return x + 1; },
    func(x) { return x * 2; },
    func(x) { return x * x; }
];

print("Array of Functions Tests\n");
print("operations[0](5) = " @ operations[0](5)); // increment
print("operations[1](5) = " @ operations[1](5)); // double
print("operations[2](5) = " @ operations[2](5)); // square

print("All pass!");
