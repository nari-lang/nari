func some_func() {
    print("generic function here")
}

// substr is now a method-only builtin, not accessible as identifier
// Test to_string with a global builtin function instead
print(to_string(print))
print(to_string(some_func))